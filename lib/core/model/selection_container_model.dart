class SelectionContainerModel {
  final String mainText, subText;
  final Function()? onPress;

  SelectionContainerModel({
    required this.mainText,
    required this.subText,
    required this.onPress,
  });
}
